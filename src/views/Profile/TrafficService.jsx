import { Fragment } from 'vue-fragment';
import { Marker, Polyline } from '@amap/amap-vue';

import { TRAFFIC_POINTS } from '@/constants';

export default {
  name: 'TrafficService',

  render() {
    return <Fragment></Fragment>;
  }
};
